package wsn_lighting.node.mobilityManager.noMobilityManager;

simple NoMobilityManager like wsn_lighting.node.mobilityManager.iMobilityManager {
 parameters:
	bool collectTraceInfo = default (false);
}
